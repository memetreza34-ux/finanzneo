const fsCleanup = await import('node:fs');
for (const s of scenes) {
  const dir = resolve(target, `03-szenen/EINZELNE-SZENEN/${s.id}`);
  const remove = (name) => fsCleanup.rmSync(resolve(dir, name), {force:true});
  if (s.type === 'image') {
    remove('animation.tsx');
    remove('remotion.md');
  } else {
    remove('bildprompt.txt');
  }
}
console.log('✓ Alte Gegentyp-Dateien aus dem kopierten Gerüst entfernt.');
