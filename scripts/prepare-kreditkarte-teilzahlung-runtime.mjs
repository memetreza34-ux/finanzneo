#!/usr/bin/env node
import {readFileSync,writeFileSync,readdirSync} from 'node:fs';import {extname,resolve} from 'node:path';
const root=resolve('reels/2026-08-10_bis_2026-08-16/samstag/reel-01_kreditkarte-teilzahlung');const timing=JSON.parse(readFileSync(resolve(root,'04-caption/word-timings.json'),'utf8'));const timeline=JSON.parse(readFileSync(resolve(root,'05-projektdateien/timeline.json'),'utf8'));
if(timing.timingStatus!=='final-audio-aligned'||!Array.isArray(timing.sentences)||!timing.sentences.length)throw new Error('BLOCKED: echte Wortzeiten fehlen.');if(!Array.isArray(timeline.scenes)||timeline.scenes.length!==10||timeline.scenes.some(s=>!Number.isFinite(s.startFrame)||!Number.isFinite(s.durationFrames)||s.durationFrames<=0))throw new Error('BLOCKED: finale Timeline fehlt.');let last=0;for(const [i,s] of timeline.scenes.entries()){if(i===0&&s.startFrame!==0)throw new Error('BLOCKED: Timeline muss bei Frame 0 starten.');if(i>0&&Math.abs(s.startFrame-last)>1)throw new Error(`BLOCKED: Lücke/Overlap vor ${s.id}`);last=s.startFrame+s.durationFrames;}
const audioDir=resolve(root,'02-audio');const allowed=new Set(['.wav','.mp3','.m4a','.aac']);const audio=readdirSync(audioDir).filter(f=>allowed.has(extname(f).toLowerCase()));if(audio.length!==1)throw new Error('BLOCKED: genau ein finales Audio erforderlich.');const words=[];for(const sentence of timing.sentences){const tokens=String(sentence.text).trim().split(/\s+/).filter(Boolean);if(!Array.isArray(sentence.frames)||sentence.frames.length!==tokens.length+1)throw new Error(`Ungültige Wortgrenzen: ${sentence.id}`);tokens.forEach((word,i)=>words.push({word,start:sentence.frames[i]/30,end:sentence.frames[i+1]/30}));}const runtime={status:'ready',sceneStarts:timeline.scenes.map(s=>s.startFrame),sceneDurations:timeline.scenes.map(s=>s.durationFrames),captionWords:words,audioSrc:`reels/kreditkarte-teilzahlung/voiceover${extname(audio[0]).toLowerCase()}`};const out=JSON.stringify(runtime,null,2)+'\n';writeFileSync(resolve(root,'05-projektdateien/runtime-data.json'),out);writeFileSync(resolve('src/reels/kreditkarte-teilzahlung/runtime-data.json'),out);console.log('✓ Kreditkarten-Runtime vorbereitet.');
