# Finales Voiceover

Hier genau **eine** finale Audio-Datei ablegen (`.wav`, `.mp3`, `.m4a` oder `.aac`).

Keine Ersatz-/Testaudios. Die finale Timeline und alle Karaoke-Wortzeiten werden ausschließlich aus diesem Audio abgeleitet.
